# Portfolio

Portfolio built with Vue 3, Vite, TypeScript, and Tailwind CSS.

## Getting Started

```bash
npm install
npm run dev
```

Open `http://localhost:3001`.

## Build

```bash
npm run build
```

The production output is generated in `dist/`.
